<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    @if (ViewBag.Title == null)
    {
        <title>Diary</title>
    }
    else
    {
        <title>@($"Diary - {ViewBag.Title}")</title>
    }
    
    <environment names="Development">
        <link rel="stylesheet" href="~/bundle/@(ViewBag.BundlePrefix ?? "layout").css" />
    </environment>
    <environment names="Staging,Production">
        <link rel="stylesheet" href="~/bundle/@(ViewBag.BundlePrefix ?? "layout").min.css" />
    </environment>
    
    @RenderSection("head", required: false)
</head>
<body>
    <div class="container@(ViewBag.Unchained ?? false ? " unchained" : "")">
        <div class="header clearfix">
            <nav>
                <ul class="nav nav-pills pull-right">
                    @Html.Partial("_LoginPartial")
                </ul>
            </nav>
            @Html.ActionLink("Diary", "Index", "Home", routeValues: null, htmlAttributes: new { id="HomeLink", @class = "h3" })
        </div>
        @RenderBody()
        <footer>
            <hr>
            <p>Created by <a href="https://gavin-tech.com/">Gavin Heise</a></p>
        </footer>
    </div>

    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" })) { }

    <environment names="Development">
        <script src="~/bundle/@(ViewBag.BundlePrefix ?? "layout").js"></script>
    </environment>
    <environment names="Staging,Production">
        <script src="~/bundle/@(ViewBag.BundlePrefix ?? "layout").min.js"></script>
    </environment>

    @RenderSection("scripts", required: false)
</body>
</html>
